{
  "name": "bidcoreai-website",
  "version": "1.1.0",
  "description": "BidcoreAI marketing website with email API and the free Federal Go/No-Go workspace",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check server.js && node --check api/routes.js && node --check api/auth.js && node --check api/db.js && node --check api/samgov.js && node --check api/scoring.js && node --check api/secretbox.js && node --check api/geocode.js && node --check public/go-no-go.js && node --check api/check-db.js",
    "db:check": "node api/check-db.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "google-auth-library": "^9.14.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.13",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
